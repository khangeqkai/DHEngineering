# Fix spec — whole-codebase review findings (2026-09-26)

All paths are relative to `jobcard-system/`. Seven executors work **in parallel in the same working tree**. Each owns a fixed set of files (below). **Never edit a file you don't own.** If a fix genuinely needs a change in someone else's file, do NOT make it — report it in your final message as "NEEDS: <file> — <change>".

## Rules for every group

- Read `/mnt/c/Users/khang/Code/DHEngineering/CLAUDE.md` first and the `docs/notes/*.md` note for your area. Follow the house rules: camelCase JS, prepared statements in `db/queries/*` or `database.js` (never inline SQL in routes), `recordHistory` with `{ field: { from, to } }` changes, present-tense actions, UTC ISO timestamps (`strftime('%Y-%m-%dT%H:%M:%fZ','now')` or `new Date().toISOString()`), `toast.error` with stable ids, field errors via `useFieldErrors` not toasts, spacing tokens not raw px, lucide icons, no `onWheel`, no runtime `<style>`.
- Match surrounding code style and comment density. Keep fixes minimal — no refactors beyond what the fix needs.
- **No schema changes** unless your group's item says so.
- **Do not run `npm install`, `npm rebuild`, `electron-rebuild` or any build** — node_modules is shared with Windows and native modules must not be rebuilt from WSL. Verify server files with `node --check <file>`. For client files, re-read your edits carefully; you may run `npx eslint <files>` only if it works without installing anything.
- Do not commit. Do not touch git state.
- If a note in `docs/notes/` becomes wrong because of your change, update it (edit only the paragraph concerned — other groups may be editing other paragraphs of the same note; if an Edit fails because the file changed, re-read and retry).
- A step that can't be done as written: report it, don't improvise.
- Final report: per item ID — what you changed (one line) or why skipped; any NEEDS lines; then `git diff --stat` output limited to your files (`git diff --stat -- <your files>`).

Already done by the coordinator (don't redo): `GET /auth/employees?includeInactive=true` returns all users each with `active: true|false`; client `api.getEmployees(includeInactive = false)`. `api.getMachines(includeInactive)` and `api.getTags(category, includeArchived)` already existed.

---

## S1 — Server: paperwork, printouts, quality forms, QA levels

**Owns:** `server/src/routes/jobcard-printout.js`, `server/src/routes/jobcard-helpers.js`, `server/src/routes/jobcard-mutations.js`, `server/src/utils/pdfFiller.js`, `server/src/utils/pdfPacket.js`, `server/src/routes/jobcard-files.js`, `server/src/routes/qa-levels.js`, `server/index.js`, `server/src/db/queries/qa*.js` (if one exists; else add the new statement wherever QA-level queries live and name that file in your report), `client/src/components/QALevelManagement.jsx` (+ its .css), `docs/notes/files-and-qa.md`.

- **S1-1 (Med)** Job card preview/printout and pre-filled QA forms show the customer company name to non-management users. Printout route `jobcard-printout.js:38`, card data `jobcard-helpers.js:456`, forms `jobcard-mutations.js:~240`. Fix: the printout/preview omits company/customer/contact for non-management requesters (`isManagement(req.user.role)`). QA forms are pre-filled once at job creation and are files on disk that workers can open — so **do not put the company name into the filled QA forms at all** (leave that field blank) unless the notes say forms must carry it; if they do, report instead of changing.
- **S1-2 (Med)** QA forms print internal codes (`ZINC_PLATE`, `DH_CAD`, `N_A`, `SAME_DAY`, `OPEN`) and ISO dates. `jobcard-helpers.js:329-347`, `pdfFiller.js:78-83`, `jobcard-mutations.js:238-244`. Fix: reuse the exact label lookup + date formatting the job-card printout uses (`jobcard-helpers.js:366-379`) so both printouts read the same.
- **S1-3 (Low)** Re-picking "Whole job" on a whole-job file renames it and writes history. `jobcard-files.js:530`. Fix: the "already this owner" check must also match the whole-job case → no-op.
- **S1-4 (Low)** QA template upload accepts any file type. `qa-levels.js:~290`. Fix: reject non-`.pdf` (extension and, if cheap, `%PDF` magic bytes) with a 400 and plain message.
- **S1-5 (Low)** Oversized template upload shows "Internal server error". `index.js:123` error handler, body limit `index.js:58`, `qa-levels.js:274`, client `QALevelManagement.jsx:151`. Fix: error handler maps `err.type === 'entity.too.large'` / status 413 to a 413 "This file is too large" and body-parse errors (`entity.parse.failed`, status 400) to 400 — never leak details; client checks size before sending using the same limit the job-file upload uses (find it in the paperwork upload code and reuse the constant/value).
- **S1-6 (Med)** Renaming a QA level doesn't reach jobs already on it; the CRITICAL checklist is decided by the name copied onto the job. `qa-levels.js:198`, `jobcard-mutations.js:320`, `utils/timeEntryHelpers.js:181-183` (not yours — don't edit). Jobs have a `qa_level_id` column. Fix: in the rename route, inside the same transaction, also update the copied level name on every job whose `qa_level_id` is that level (new prepared statement). Record one history entry on the level as today. That makes the copied name always current, so the checklist and job list follow the level.
- **S1-7 (Low)** QA Levels page: the clickable level name (rename) isn't a button; the icon-only delete button has no accessible name. `QALevelManagement.jsx:242-249, 270`. Fix: real `<button type="button">` with aria-label; strip chrome in CSS.
- **S1-8 (Low)** Print packet ignores phone photo EXIF rotation. `pdfPacket.js:28`. Fix: apply rotation (e.g. sharp's `.rotate()` with no args) before converting.

## S2 — Server: time entries, timers, suppliers, validation

**Owns:** `server/src/utils/startTimerUndo.js`, `server/src/utils/timeEntryHelpers.js`, `server/src/middleware/validation.js`, `server/src/routes/jobcard-time-entries.js`, `server/src/routes/suppliers.js`, `server/src/db/queries/` statement additions for suppliers/time entries (put them in the file where those queries already live), `docs/notes/time-and-costing.md`.

- **S2-1 (Low)** Start-timer undo reverts the job to Open while another worker's timer is still running on it. `startTimerUndo.js:38-44`, called from `jobcard-time-entries.js:186`. Fix: skip the status revert if any other running timer exists on the job.
- **S2-2 (Low)** 31-day block-length check blocks a worker saving pieces on a long-running stopped timer. `timeEntryHelpers.js:231-238`, `jobcard-time-entries.js:383-386, 406-409`; note says the limit is for hand-entered blocks only. Fix: apply the 31-day check only when the start/end times are actually being changed by the request (or on hand-entered create), not when the stored times are reused.
- **S2-3 (Low)** A hand-added running block with a future start is silently discarded on Stop as "under 15 seconds". `validation.js:400-416`, `startTimerUndo.js:25-26`. Fix: reject a start time in the future (allow ~1 minute of clock skew) on hand-added/edited blocks, with a plain message; and the <15s discard must require a non-negative duration (a negative duration is never auto-discarded).
- **S2-4 (Low)** History for hand-added and deleted time blocks is thin. `jobcard-time-entries.js:326, 600`. Fix: include end time, pieces/qty, scrap, part and (on delete) worker in the `{field:{from,to}}` changes (for create: from null; for delete: to null).
- **S2-5 (Low)** Scrap values like "-3", "2.5", "3 pcs" are silently coerced. `jobcard-time-entries.js:258-259`. Fix: validate scrap (and qty) as non-negative whole numbers server-side; reject with 400 otherwise.
- **S2-6 (Low)** Invoiced-job time edits: nothing server-side to change — the server correctly refuses (`timeEntryHelpers.js:143`). Just make sure its refusal message says the job must be reopened first (plain words). Client side is C2's job.
- **S2-7 (Low)** Supplier rename doesn't reach the supplier name copied onto parts; stored name never checked against the id. `suppliers.js:179-198`, `validation.js:474-485`. Fix: in the rename route's transaction, update the copied supplier name on every part whose supplier id is that supplier (new prepared statement). On part save, when a supplier id is given, store the supplier's current name from the DB rather than the name the client sent.

## S3 — Server: statistics, settings, serving, machines, search, tags, users

**Owns:** `server/src/routes/statistics.js`, `server/src/routes/statistics-helpers.js`, `server/src/routes/settings.js`, `server/src/utils/certs.js`, `server/src/utils/mdnsResponder.js`, `server/src/utils/netHost.js`, `server/src/routes/machines.js`, `server/src/routes/search.js`, `server/src/routes/tags.js`, `server/src/routes/auth.js` (the coordinator already edited `/employees` — leave that), `server/src/routes/jobcard-notes.js`, `server/src/routes/companies.js`, `server/src/db/queries/entities.js`, `server/src/db/queries/operations.js`, `docs/notes/jobs-and-status.md`, `docs/notes/serving-and-packaging.md`, `docs/notes/customers-and-tags.md`.

- **S3-1 (Med)** Statistics uses the invoice date as a job's finish day, so an on-time job flips to late when invoiced. `statistics-helpers.js:165`, `statistics.js:199, 210`. Fix: finish day = end of the last logged work; fall back to the invoice/done date only when there is no logged work. Keep the timezone handling the notes require.
- **S3-2 (Low)** The job-number start can be set back onto a deleted job's number. `settings.js:175`; rule in `docs/notes/jobs-and-status.md:7`. Fix: refuse a starting number lower than the current counter (as well as existing jobs), with a plain message.
- **S3-3 (Med)** HTTPS certificate is never renewed on expiry. `certs.js:29, 211-221`. Fix: on startup, also regenerate when the leaf certificate expires within 30 days (read its notAfter). Read `docs/notes/serving-and-packaging.md` first — if other PCs trust a local CA, reuse the CA so nothing needs reinstalling; if the CA itself is near expiry, report that as NEEDS rather than inventing a scheme.
- **S3-4 (Low)** mDNS keeps answering with the startup address after the network changes. `mdnsResponder.js:27, 47`, `netHost.js:43`. Fix: work out the current address per query (or refresh the interface list periodically/on each answer).
- **S3-5 (Low)** Renumbering a machine leaves past time blocks under the old number text. `machines.js:92-99`, stats group `statistics.js:300-307`, search `search.js:335`. Fix: in the update route, if the machine number changes and time entries reference the old number, update those references in the same transaction if the stored format allows a safe exact rewrite (inspect how machines are stored on a time entry). If it's a free-text list that can't be rewritten safely, instead refuse the renumber with "This machine has logged work; archive it and add a new one instead." Report which you did.
- **S3-6 (Low)** Search LIKE patterns don't escape `%` and `_`. `search.js:146, 204, 256, 266, 283` — copy the escaping already used at `search.js:294-297` (with `ESCAPE`).
- **S3-7 (Low)** Search QA-level filter compares against the name copied on the job. `search.js:226`. Fix: filter by `qa_level_id` when the client sends a level id param (`qaLevelId`); keep accepting nothing else. (C4 will send `qaLevelId`.) Tell in report the exact param name used.
- **S3-8 (Low)** Tag restore renames the option silently. `tags.js:118-123`. Fix: when the restore also changes the name, record `name: { from, to }` in the history changes.
- **S3-9 (Low)** Comment author name frozen after a user rename. `jobcard-notes.js:40-46`, `operations.js:146-147`, `auth.js:418`. Fix: in the user-update route, when the name changes, update the copied author name on that user's job notes in the same transaction (new prepared statement), if notes store the author's user id. If they don't store the id, report instead.
- **S3-10 (Low)** Machine duplicate check is case-sensitive (`entities.js:231`) and company duplicate check is case-insensitive (`entities.js:72`). Make the machine active-number check case-insensitive (`COLLATE NOCASE`) so "cnc-01" and "CNC-01" collide. Leave companies as is.

## C1 — Client: job list and Excel export

**Owns:** `client/src/components/JobCardList.jsx` (+css), `client/src/components/JobCardListFilters.jsx`, `client/src/components/JobCardColumnsMenu.jsx`, `client/src/hooks/useJobCardColumnOrder.js`, `client/src/hooks/useJobCardSort.js`, `client/src/utils/excelExport.js`.

- **C1-1 (Med)** Picking Invoiced from the list's status badge has no confirmation. `JobCardList.jsx:181`. Fix: same confirmation wording and mechanism the job screen uses (`jobcard/JobIdentityStrip.jsx:191` — read, don't edit; use the shared confirm dialog the app already uses).
- **C1-2 (Med)** Show Archived keeps a hidden status filter applied → empty list. `JobCardList.jsx:303-310`, `JobCardListFilters.jsx:243`. Fix: ignore the status filter in the archived view (like "My Jobs" already is).
- **C1-3 (Low)** Show Archived toggle race. `JobCardList.jsx:95-111`. Fix: ignore replies that aren't for the latest request (request counter).
- **C1-4 (Low)** Column "move earlier/later" swaps with invisible columns. `useJobCardColumnOrder.js:287-299`, `JobCardColumnsMenu.jsx:46-82`, `JobCardList.jsx:416-423`. Fix: move past columns not visible to this user/hidden, to the next visible one.
- **C1-5 (Low)** Job # sorts as text. `useJobCardSort.js:17, 57-58`. Fix: natural/numeric-aware compare (`localeCompare(b, undefined, { numeric: true })`).
- **C1-6 (Low)** Excel export silently drops jobs whose fetch failed. `excelExport.js:320-340, 383-388`. Fix: count failures and surface them (return value or toast the caller already shows) — "N jobs couldn't be loaded and are missing from the export".
- **C1-7 (Low)** Excel option labels rebuilt from the stored value ("Ptfe Coating"). `excelExport.js:75-83`. Fix: use the option's real label from the loaded tag lists (include archived) and fall back to the rebuilt text only when not found.
- **C1-8 (Low)** Late Jobs sheet shows raw ISO dates and raw status codes. `excelExport.js:479-487`. Fix: use the app's date formatter and status label map.

## C2 — Client: job screen time & timers

**Owns:** `client/src/components/jobcard/useTimeEntries.js`, `client/src/components/jobcard/tabs/TimeEntryForm.jsx`, `client/src/components/jobcard/tabs/TimeEntryCard.jsx`, `client/src/components/jobcard/mappers.js`, `client/src/components/jobcard/useTimer.js`, `client/src/components/jobcard/useJobCardTimerActions.js`, `client/src/components/jobcard/JobCardModal.jsx`, `client/src/hooks/useActiveTimerIndicator.js`, `client/src/components/jobcard/LineItemTimerButton.jsx`, `client/src/components/jobcard/useSaveQueue.js`, `client/src/components/common/CheckboxDropdown.jsx`.

- **C2-1 (Med)** Time-entry Machines box hides an archived machine still on the entry. `JobCardModal.jsx:111`, `TimeEntryForm.jsx:98-112`, `CheckboxDropdown.jsx:35-39`; pattern to copy: `jobcard/tabs/LineItemTagSelect.jsx:90-97`. Fix: load machines with `api.getMachines(true)`; list active machines plus any archived machine already on the entry, labelled "(retired)", so it can be unticked; archived machines not on the entry are not offered.
- **C2-2 (Low)** Worker dropdown on an archived worker's block shows "Select worker…". `TimeEntryForm.jsx:73`. Fix: same pattern — include the block's owner marked "(archived)" (source list via `api.getEmployees(true)` where the modal loads employees, or fall back to the entry's stored worker name).
- **C2-3 (Low)** Invoiced job: time editing asks "this will update the final total" then the server refuses; timer buttons live. `useTimeEntries.js:108`, `JobCardModal.jsx:260`. Fix: on an invoiced/archived job, remove the misleading confirmation; disable add/edit/delete time and Start/Resume with a hint "Reopen the job to change its time" (Stop must still work). Look for where the part timer buttons get their disabled state; if that's in a file you don't own, report NEEDS.
- **C2-4 (Low)** Editing a block truncates seconds. `mappers.js:87`, `useTimeEntries.js:60-61, 99-103, 125-126`. Fix: if the user didn't change the start/finish field, send the original stored timestamp unchanged; only a changed field is rebuilt from the minute-precision input.
- **C2-5 (Low)** Scrap boxes accept any text. `useTimeEntries.js:25-26`, `TimeEntryForm.jsx:118-124`. Fix: digits only, same as Qty.
- **C2-6 (Low)** Running clock can go negative on clock skew. `useTimer.js:68-72`, `useActiveTimerIndicator.js:37-40`, `LineItemTimerButton.jsx:6-11`. Fix: clamp elapsed at 0 (as `TimeEntryCard.jsx:15` does).
- **C2-7 (Low)** Late timer/status/time-list replies from a previous job land on the next job. `useJobCardTimerActions.js:77-79, 92-93`, `JobCardModal.jsx:217-236`. Fix: capture the job id at request time and drop the reply if the open job changed.
- **C2-8 (Low)** "Stop & Start" timer switch doesn't tick the worker's assignee tile. `useTimer.js:362-374`, `useJobCardTimerActions.js:42-45, 76`. Fix: the switch path runs the same "worker now assigned" update as a direct start.
- **C2-9 (Low)** Save-queue "still saving?" matches by key prefix, so worker #1 collides with #12. `useSaveQueue.js:180-183` (callers `useJobCardForm.js:319, 328` — not yours). Fix: exact key match, or prefix + separator so `assignee:1` never matches `assignee:12`. Check the key format used by callers and keep them working without editing them.

## C3 — Client: job screen details, header, new job, customer box

**Owns:** `client/src/components/jobcard/useInstantSave.js`, `client/src/components/jobcard/useInstantItems.js`, `client/src/components/jobcard/useJobCardForm.js`, `client/src/components/jobcard/useJobCardSave.js`, `client/src/components/jobcard/useUnsavedGuard.js`, `client/src/components/jobcard/JobIdentityStrip.jsx` (+css), `client/src/components/jobcard/tabs/DetailsTab.jsx` (+css), `client/src/components/jobcard/useContactSearch.js`, `client/src/components/jobcard/jobCardContact.jsx`, `client/src/components/jobcard/jobCardValidation.mjs`, `client/src/components/jobcard/useJobSearch.js`, `client/src/components/common/CalendarPicker.jsx` (+css), `client/src/components/jobcard/tabs/ItemsTab.jsx`.

- **C3-1 (Med)** New job: blurring the company box re-capitalises a picked customer's name, which drops the pick → create refused as duplicate. `DetailsTab.jsx:256-259`, `useContactSearch.js:161-178`, `jobCardContact.jsx:33-49`. Fix: don't re-format the name when it came from a pick (a just-picked guard like the previous-job box), and/or when the typed name case-insensitively matches an existing customer, treat it as that customer instead of offering to create a new one.
- **C3-2 (Low)** Customer list loaded once and never refreshed. `useContactSearch.js:39, 45-54, 223-229`. Fix: reload the list each time a new job is opened (clear the loaded flag in the per-open reset).
- **C3-3 (Med)** Previous-job suggestions exclude invoiced (archived) jobs. `useJobSearch.js:22`. Fix: load active + archived jobs (the list route has an archived option — see `server/src/routes/jobcards.js:38`, read only) and merge.
- **C3-4 (Low)** Changing a field and quickly changing it back saves the first change. `useInstantSave.js:71-74`, `useJobCardForm.js:268-282`, `useInstantItems.js:331-339, 132-137`. Fix: the "same as stored, skip" shortcut must not apply while a different value for that field is in flight — queue the revert so the last choice is what's stored.
- **C3-5 (Low)** Slow create + "Close anyway" → the late reply closes whichever job is open. `useJobCardSave.js:73-78`, `useUnsavedGuard.js:48-58`. Fix: capture a token for the creating session; if the modal was closed/reopened since, don't close it or set warnings (still refresh the list).
- **C3-6 (Low)** Due date can't be cleared. `JobIdentityStrip.jsx:456-460`, `CalendarPicker.jsx:97-101`. Fix: a "Clear" action in the picker (only where the field is optional) that saves an empty due date.
- **C3-7 (Low)** Due date shows no year. `JobIdentityStrip.jsx:133`. Fix: include the year when it isn't the current year.
- **C3-8 (Low)** Archived worker stays assigned with no tile. `DetailsTab.jsx:564`. Fix: load employees with `api.getEmployees(true)` where the tiles' list comes from (find the loader; if it's in a file you don't own, report NEEDS); show active workers plus any archived worker currently assigned, marked "(archived)", so they can be unticked; archived workers not assigned are not shown.
- **C3-9 (Low)** New-job validation names internal part numbers. `jobCardValidation.mjs:46-50`, `useJobCardForm.js:170-175`, `ItemsTab.jsx:298`. Fix: messages use the part's position in the list (index + 1), matching the screen. Fix the wrong code comment too.

## C4 — Client: search, statistics, settings, labour rates, management pages, camera/paperwork

**Owns:** `client/src/components/SearchPage.jsx`, `client/src/hooks/useSearch.js`, `client/src/components/statistics/OnTimeTab.jsx`, `client/src/components/statistics/MachinesTab.jsx`, `client/src/hooks/useLabourRates.js`, `client/src/components/LabourRatesSettings.jsx`, `client/src/hooks/useSettings.js`, `client/src/components/settings/*`, `client/src/utils/formatters.js`, `client/src/components/UserManagement.jsx`, `client/src/components/ContactManagement.jsx`, `client/src/components/SupplierManagement.jsx`, `client/src/components/common/DataTable.jsx`, `client/src/components/TagManagement.jsx`.

- **C4-1 (Med)** Search filters list only active workers/machines/job types. `useSearch.js:55-58`, `SearchPage.jsx:292, 331, 362, 369`. Fix: load with `api.getEmployees(true)`, `api.getMachines(true)`, `api.getTags('job_type', true)`; show archived entries after active ones marked "(archived)"/"(retired)".
- **C4-2 (Low)** Search status labels wrong ("Awaiting Material", "Po Requested", "Cust Notified"). `SearchPage.jsx:33, 63, 277`. Fix: use the app's canonical status label map (find where the job list/job screen gets status labels and import it).
- **C4-3 (Low)** Search QA filter by name misses renamed levels. `SearchPage.jsx:298`. Fix: send the level id as `qaLevelId` (S3 adds server support with that param name).
- **C4-4 (Low)** Activity row about a customer company isn't clickable. `SearchPage.jsx:130-145`. Fix: open the Customers page like the contact case does.
- **C4-5 (Low)** Statistics Late Jobs list shows raw ISO dates. `OnTimeTab.jsx:117-126`. Fix: app date formatter.
- **C4-6 (Low)** Busiest Machine names a machine with 0h; blank name shows blank. `MachinesTab.jsx:25, 64`. Fix: show a dash when top hours are 0; fall back to the machine number when name is empty.
- **C4-7 (Med)** Labour Rates: saving one card reloads everything and wipes other cards' unsaved edits (and flashes "Loading…"). `useLabourRates.js:113-134, 176, 203, 232, 254, 272`, `LabourRatesSettings.jsx:13`. Fix: after a card saves, update only that card's saved values from the reply/its own data; don't reset other cards and don't flip the page-level loading flag.
- **C4-8 (Low)** Settings: same wipe. `useSettings.js:46-67, 107, 122, 135, 148, 166`. Same fix.
- **C4-9 (Low)** Inactivity timeout box clamps every keystroke. `settings/SecurityCard.jsx:24`. Fix: allow empty/partial while typing; validate 1–60 on save (field error, not toast).
- **C4-10 (Low)** `toTitleCase` capitalises after an apostrophe. `formatters.js:3`. Fix: "Bob's" stays "Bob's"; "O'Brien" should stay "O'Brien" — only lowercase a single letter after an apostrophe when it's followed by a word boundary (e.g. `'s`, `'t`, `'d`, `'ll`, `'re`, `'ve`, `'m`).
- **C4-11 (Low)** "Export Current View" ignores the table's search. `UserManagement.jsx:199`, `ContactManagement.jsx:222`, `SupplierManagement.jsx:234`. Fix: export the filtered rows the table shows.
- **C4-12 (Low)** Empty search shows "No X yet — add your first". `DataTable.jsx:80-82`, `ContactManagement.jsx:364-370`. Fix: when a search term is active and there are rows overall, say "No matches for “term”".
- **C4-13 (Low)** Enter saves an untidied machine/company name. `TagManagement.jsx:99-106, 240`, `ContactManagement.jsx:66-76, 247-250`. Fix: apply the same formatter inside the save handler before sending.
- **C4-14 (Med)** Suppliers: retiring a service unticks it from the supplier being edited → saving drops it. `SupplierManagement.jsx:178-193` (message at :181). Fix: retiring leaves the service ticked on the supplier being edited (show it "(retired)").

## C5 — Client: paperwork camera and photo files

**Owns:** `client/src/components/jobcard/HubCameraView.jsx`, `client/src/components/jobcard/JobPaperworkHub.jsx`, `client/src/components/jobcard/useJobFiles.js`, `client/src/components/jobcard/useCamera.js`.

- **C5-1 (Med)** Taking a photo while a save is running → the new photo is lost when the save clears the strip. `HubCameraView.jsx:24`, `JobPaperworkHub.jsx:186`, `useJobFiles.js:196`. Fix: remove only the photos that were saved (by identity), not the whole strip.
- **C5-2 (Med)** Partial upload failure → retry re-uploads photos that already went up. `useJobFiles.js:188-199`, `JobPaperworkHub.jsx:186-187`. Fix: remove each photo from the strip as soon as it uploads; on failure the strip holds only the unsent ones; error toast says how many didn't save.
- **C5-3 (Low)** Back during camera start-up leaves the camera on and later stuck black. `useCamera.js:33-53, 71-83`. Fix: a start that resolves after stop/unmount stops its stream immediately; re-attaching the stream to a new video element must work even if the camera was already "on".
- **C5-4 (Low)** Photo names use UTC. `useJobFiles.js:186-187`. Fix: build the name from local date/time parts.
